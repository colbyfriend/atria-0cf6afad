# Intermediate raw connector dumps — regenerated by the refresh routine
_data/*/raw/
# Local dev-only static server
_data/serve.cjs
# Icon source (already rasterized to PNGs)
_data/icon.svg
.DS_Store
